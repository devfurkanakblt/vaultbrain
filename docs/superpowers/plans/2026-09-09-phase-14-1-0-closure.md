# Phase 14 — 1.0 Closure Implementation Plan

**Goal:** Close the remaining synchronization, re-key, format-governance and
release-readiness obligations without hiding unfinished work from earlier phases.

**Baseline:** Phase 12 has been merged to `main` according to the project owner.
The implementing agent must verify the actual branch and commit state before making
changes. Phase 12 release publication and production signing operations are outside
this plan.

**Primary sources:** `docs/ROADMAP.md`, `README.md`, `SECURITY.md`,
`docs/ARCHITECTURE.md`, `docs/FORMAT-1.0.md`, `docs/AUDIT-SCOPE.md`, and the
existing phase plans and specifications under `docs/superpowers/`.

## Global constraints

- Read `AGENTS.md` and inspect the current code before changing it. When an existing
  graph is available, use Graphify queries for codebase investigation; update the
  graph after code changes.
- Preserve older vault readability and every committed compatibility fixture.
- Work test-first for behavior changes. Each subphase must be independently
  reviewable and must not rely on a later subphase to restore safety.
- Do not generate production signing material, modify repository secrets, publish a
  release, create a release tag, or push without explicit authorization.
- Never mark external audit, cross-platform validation or real-device acceptance as
  complete based only on mocks or synthetic CI evidence.
- Keep earlier roadmap items traceable. When ownership moves here, annotate the old
  item with its Phase 14 destination; close both only after the new acceptance gate
  passes.
- Avoid parallel implementations of the same protocol behavior. Define one canonical
  owner and make all other surfaces call or verify it.
- Keep passphrases, keys and plaintext vault content out of logs, process arguments,
  configuration, crash journals and unprotected temporary files.

## Phase 14.0 — Roadmap reconciliation

### Task 1: Establish the verified baseline

- [ ] Confirm Phase 12 is present on `main` and record the exact baseline commit.
- [ ] Confirm the worktree is clean or document every pre-existing user change.
- [ ] Inventory every unchecked item in `docs/ROADMAP.md` and map it to code, tests,
      documentation and an owning phase.
- [ ] Classify each item as genuinely incomplete, implemented but stale in the
      documentation, externally gated, or awaiting a product decision.

### Task 2: Reconcile Phase 7.7 and stale documentation

- [ ] Verify all five Phase 7.7 corrections in implementation and focused tests:
      retention-policy classification, Rust `legacyChangeIdentity` preservation,
      authenticated re-key audit events, corrected unsupported-keyset/recovery
      behavior, and verification against current and retiring keys.
- [ ] Mark only verified Phase 7.7 items complete.
- [ ] Correct README language that still describes shipped re-key, recovery-slot or
      desktop passphrase functionality as unfinished.
- [ ] Add Phase 14 and ownership links to `docs/ROADMAP.md` without deleting the
      historical origin of an unfinished requirement.

### Acceptance gate

- [ ] Every roadmap checkbox has one unambiguous owner and evidence state.
- [ ] No implemented feature remains described as missing, and no missing feature is
      described as complete.
- [ ] Documentation-only changes pass formatting and link checks.

## Phase 14.1 — Complete multi-device sync

### Task 1: Portable workspace-state contract

- [ ] Write a focused design specification for the portable representation of saved
      views, bookmarks and layouts.
- [ ] Decide and document the canonical owner across the TypeScript sync core and the
      Rust-owned `workspace.enc` surface.
- [ ] Define versioning, conflict semantics, causal ordering, size limits and
      fail-closed validation before implementation.
- [ ] Prove that a second device cannot silently lose supported workspace state.

### Task 2: Workspace-state synchronization

- [ ] Add failing cross-device tests for saved views, bookmarks and layouts.
- [ ] Capture local workspace mutations in the encrypted immutable change protocol.
- [ ] Apply valid remote workspace changes to live storage transactionally.
- [ ] Cover concurrent edits, duplicates, replay, malformed payloads, rollback and
      crash recovery.
- [ ] Preserve compatibility with vaults and peers that predate portable workspace
      state.

### Task 3: Desktop-driven device lifecycle

- [ ] Add owner-controlled desktop enrollment with explicit identity/fingerprint
      verification.
- [ ] Add owner-controlled revocation with the existing epoch-rotation guarantees.
- [ ] Add desktop relay push/pull with bounded operations, cancellation, useful
      progress and safe retry behavior.
- [ ] Enforce unlock state, capability scope and user confirmation in the native
      command boundary; do not expose arbitrary URLs, keys or paths to the frontend.
- [ ] Test stale responses, concurrent operations, revoked devices, hostile relay
      data, offline behavior and secret-free error reporting.

### Acceptance gate

- [ ] Two desktop instances can enroll, exchange all supported portable state,
      revoke a device and continue syncing under the new epoch.
- [ ] Notes, canvases, attachments, plugin policy, saved views, bookmarks and layouts
      survive the documented recovery drill.
- [ ] Focused TypeScript, Rust and desktop tests pass on all supported platforms.
- [ ] The external security-audit checkbox remains open until an independent audit is
      actually completed.

## Phase 14.2 — Re-key aftermath

### Task 1: Attachment-identity migration design

- [ ] Write and review a dedicated specification before implementation.
- [ ] Enumerate every reference affected by rotating `attachmentId`: attachment
      directories, active canvases, canvas history, derived indexes and sync changes.
- [ ] Define how rotated sync-change IDs rewrite descendant `parent` references while
      preserving a valid causal DAG.
- [ ] Define a multi-device/version-transition rule that prevents peers from deriving
      divergent identities.
- [ ] Define interruption, resume, rollback, space-budget and stale-journal behavior.

### Task 2: Atomic and resumable identity migration

- [ ] Add failure-injection tests at every durable transition.
- [ ] Stage and verify the complete rewritten vault before changing live identity.
- [ ] Make replay after interruption idempotent.
- [ ] Fail closed on unknown encrypted artifacts, missing references, symlinks,
      malformed history or an inconsistent change DAG.
- [ ] Verify old and new fixtures, cross-core reads and multi-device compatibility.

### Task 3: Deliberate lock-break path

- [ ] Specify the command, threat model, owner confirmation and audit record.
- [ ] Distinguish a live lock from a stale or abandoned lock using the strongest
      platform evidence available.
- [ ] Refuse ambiguous cases by default and provide a deliberate override only when
      its consequences are made explicit.
- [ ] Test live-owner refusal, stale-lock recovery, malformed lock metadata, PID reuse,
      concurrent attempts and crash recovery.

### Acceptance gate

- [ ] A fully populated synchronized vault can re-key and rotate affected identities
      without losing content, history, links or DAG validity.
- [ ] Randomly interrupted migrations resume or roll back to a complete vault.
- [ ] A live lock cannot be broken accidentally in the supported threat model.

## Phase 14.3 — Core and format consolidation

### Task 1: Retire the duplicate sync change log

- [ ] Prove production call paths use the canonical implementation in `src/sync.ts`.
- [ ] Compare the duplicate `src/sync/change-log.ts` behavior and tests against the
      canonical path.
- [ ] Move any required invariant or regression test to the canonical implementation.
- [ ] Remove the duplicate implementation and tests that validate only dead code.

### Task 2: Freeze domain separation

- [ ] Inventory every cryptographic domain-separation/AAD constant in both cores.
- [ ] Add `secondbrain-vault:kv:v2`,
      `secondbrain-vault:sync-local-transaction:v1` and
      `secondbrain-vault:sync-apply-receipt:v1` to the frozen canonical inventory.
- [ ] Extend conformance tests so an accidental rename, omission or divergent
      cross-core value fails CI.

### Task 3: Close the encrypted-artifact catalogue gaps

- [ ] Add `documents/index.enc` and `documents/plugin-policy.enc` to the applicable
      format catalogue and compatibility policy.
- [ ] Enumerate every in-vault encrypted artifact from the canonical inventory.
- [ ] Add a CI test proving each encrypted artifact has a format declaration,
      compatibility policy and explicit re-key classification.
- [ ] Make addition of an unclassified encrypted artifact fail before merge.

### Acceptance gate

- [ ] There is one production implementation for each sync protocol responsibility.
- [ ] Domain-separation values and encrypted artifacts are completely frozen by
      executable conformance tests.
- [ ] TypeScript and Rust agree on every shared format surface they both implement.

## Phase 14.4 — Personal-memory disposition

### Task 1: Ownership audit

- [ ] Trace all production imports and call paths into `src/memory/`.
- [ ] Compare the existing modules with the Phase 7.6 personal-memory plan's security,
      durability, lifecycle, UI, MCP and re-key requirements.
- [ ] Record what is implemented, stubbed, unreachable or unsupported.

### Task 2: Product decision

- [ ] Write a short decision record comparing two explicit options:
      productize personal memory as a later phase, or remove the unused modules and
      their product-facing claims.
- [ ] Evaluate user value, privacy boundaries, platform dependencies, maintenance
      cost and unfinished security work.
- [ ] Obtain explicit product-owner approval. Do not infer the decision from existing
      stubs.

### Acceptance gate

- [ ] Personal-memory code has an explicit owner and roadmap destination, or unused
      modules have been deliberately removed.
- [ ] If productization is selected, create a separate Phase 15 specification and
      implementation plan; do not expand Phase 14 into that implementation.

## Phase 14.5 — Independent audit and 1.0 readiness

### Task 1: Audit handoff

- [ ] Update `docs/AUDIT-SCOPE.md` to match the final implemented surface.
- [ ] Freeze the reviewed commit and reproducible build/test instructions.
- [ ] Complete an independent security audit.
- [ ] Resolve each finding or record an explicit, durable acceptance with rationale.

### Task 2: Final verification

- [ ] Run lint, formatting, type checks, the complete Node and desktop suites, desktop
      production build, Rust formatting/clippy/tests, format conformance, recovery
      drill and required 1k/10k/100k performance gates.
- [ ] Run native credential-store and filesystem/durability checks on their real
      Windows, macOS and Linux hosts.
- [ ] Confirm every earlier roadmap item is completed, explicitly declined or owned
      by a named later phase.
- [ ] Publish a 1.0-readiness report containing exact commands, results, platform
      evidence, accepted limitations and unresolved blockers.

### Acceptance gate

- [ ] Independent audit evidence exists and all findings have dispositions.
- [ ] All required platform and compatibility gates pass on the reviewed commit.
- [ ] The roadmap, README, security model, architecture and format documents describe
      the same shipped product.
- [ ] Phase 14 is not marked complete while any required external evidence or earlier
      source requirement remains open.

## Required verification commands

Run the commands applicable to each subphase, and run the complete set before final
acceptance:

```bash
npm run lint
npm run format:check
npm run typecheck
npm test
npm run test:platform-artifacts
npm run test:release
npm run test:update-acceptance
npm run desktop:test
npm run desktop:build
npm run quality:rust
npm run recovery:drill
npm run benchmark
npm run benchmark:10k
npm run benchmark:100k
```

Run `graphify update .` after code changes when Graphify is available. Record missing
tools or unavailable platforms as verification gaps; do not reinterpret them as
passing results.

## Handoff format for every subphase

The implementing agent must report:

- completed checklist items and their evidence;
- changed files and user-visible behavior;
- tests added and exact command results;
- compatibility, security and migration risks reviewed;
- platform or external evidence still pending;
- roadmap items that must remain open; and
- the next smallest safe step.

