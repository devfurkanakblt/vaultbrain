# These files are deterministic, public cross-core test vectors. Their
# passphrase, keys and ciphertext are deliberately dummy values generated by
# scripts/make-keyring-legacy-vector.mjs; they are not credentials.
[allowlist]
description = "Deterministic legacy keyring interoperability vector"
paths = [
  '''^scripts/make-keyring-legacy-vector\.mjs$''',
  '''^test/fixtures/keyring-legacy-vector\.json$''',
]
